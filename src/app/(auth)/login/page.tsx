import Authentication from "@/components/auth/Authentication";

import React from "react";

const page = async () => {
  return (
    <>
      <Authentication />
    </>
  );
};

export default page;
